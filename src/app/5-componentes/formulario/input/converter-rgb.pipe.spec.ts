import { ConverterRgbPipe } from './converter-rgb.pipe';

describe('ConverterRgbPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverterRgbPipe();
    expect(pipe).toBeTruthy();
  });
});
